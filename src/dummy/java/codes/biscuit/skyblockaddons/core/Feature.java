package codes.biscuit.skyblockaddons.core;

public enum Feature {
    COMPACT_TAB_LIST(152, "settings.compactTabList", false);

    Feature(int id, String translationKey, boolean defaultDisabled) {
    }
}
